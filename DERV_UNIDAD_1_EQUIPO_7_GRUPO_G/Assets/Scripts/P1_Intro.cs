using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P1_Intro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int alan;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
